<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Postes List</title>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">


  <style>
      th, td {
          text-align: center;
      }

      tr:nth-child(odd) {
          background-color: #41e10b;
      }

      tr:nth-child(even) {
          background-color: #2e4786;
      }
  </style>
</head>
<body>

<div class="container mt-5">
  <h1>Postes List</h1>

  <!-- جدول Bootstrap -->
  <table class="table">
    <!-- سرصد جدول -->
    <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Phone</th>
      <th>Email</th>
      <th>Twitter</th>
      <th>Actions</th> <!-- ستون جدید برای لینک‌ها -->
    </tr>
    </thead>
    <!-- بدنه جدول -->
    <tbody>
    <% @postes.each do |poste| %>
      <tr>
        <td><%= poste.first_name %></td>
        <td><%= poste.last_name %></td>
        <td><%= poste.phone %></td>
        <td><%= poste.email %></td>
        <td><%= poste.twitter %></td>
        <td>
          <%= link_to "Show", poste, class: "btn btn-info btn-sm" %>
          <%= link_to "Edit", edit_poste_path(poste), class: "btn btn-warning btn-sm" %>
          <%= link_to "Destroy", poste, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger btn-sm" %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <%= link_to "New Poste", new_poste_path, class: "btn btn-primary" %>
</div>

</body>
</html>
